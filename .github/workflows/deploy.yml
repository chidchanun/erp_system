name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd /home/ChidchanunServer/erp_system
          git pull origin main
          npm install
          npm run build
          pm2 restart mainWeb
