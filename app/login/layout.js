export const metadata = {
  title: "Login Page",
  description: "Login into ERP System",
};


export default function LoginLayout({children}) {
    return (
        <div>
            {children}
        </div>
    )
}